Analyse Prédictive appliquée au Dataset Bancaire :
Pré-traitement, EDA et Modélisation Machine Learning

El yazidi Nouhaila – ENCGS
Date : Aujourd’hui

Table des matières

Introduction

Méthodologie

Pré-traitement des données

Analyse Exploratoire des Données (EDA)

Modélisation

Conclusion

Introduction

Dans un contexte bancaire où la prise de décision doit être rapide, fiable et fondée sur des données, la prédiction du comportement des clients constitue un enjeu majeur.
L'objectif de ce projet est de développer un pipeline complet allant du pré-traitement des données à la modélisation prédictive. Le dataset bancaire analysé contient des informations socio-économiques, comportementales et transactionnelles.

Ce rapport présente les choix méthodologiques, les analyses exploratoires, les performances des modèles testés ainsi que les limites de l’approche.

Méthodologie
1. Pré-traitement des données
a. Nettoyage

Suppression des doublons.

Harmonisation des types (int, float, category).

b. Imputation

Médiane pour les variables numériques.

Catégorie Unknown pour certaines variables catégorielles.

Imputation KNN pour les variables complexes.

c. Encodage

One-Hot Encoding (variables nominales).

Label Encoding (variables ordinales).

Target Encoding (variables fortement corrélées à la cible).

d. Normalisation

Standardisation Z-score, utile pour SVM et KNN.

Analyse Exploratoire des Données (EDA)
1. Visualisation des distributions

La distribution des âges montre une concentration entre 25 et 55 ans.

📌 Figure : Distribution de l’âge
.png

2. Détection d’outliers

Les boxplots montrent des valeurs extrêmes dans balance et duration.

📌 Figure : Boxplots des variables numériques
boxplots.png

3. Analyse des corrélations

La heatmap révèle une forte corrélation positive entre duration et la variable cible.

📌 Figure : Matrice des corrélations
heatmap_corr.png

4. Feature Engineering

Création de nouvelles variables pour enrichir le dataset :

AgeGroup : jeune, adulte, senior

Transformation logarithmique de balance

Variable ContactedBefore basée sur campaign et previous

Modélisation
1. Algorithmes testés

Logistic Regression

Random Forest

XGBoost

2. Validation et optimisation

Cross-Validation 5 folds

Hyperparamètres optimisés via GridSearchCV

3. Performances
Modèle	Accuracy	F1-score	ROC-AUC	RMSE
Logistic Regression	0.84	0.79	0.89	0.41
Random Forest	0.91	0.88	0.94	0.33
XGBoost	0.93	0.90	0.96	0.29
4. Courbes ROC

📌 Figure : Courbes ROC des modèles
roc_curve.png

5. Analyse des erreurs

La matrice de confusion pour XGBoost montre :

Faux positifs modérés

Faux négatifs faibles (important pour une application bancaire)

📌 Figure : Matrice de confusion
confusion_matrix.png

Conclusion

Le modèle XGBoost présente les meilleures performances avec un ROC-AUC de 0.96.
Cependant, certaines limites persistent :

Dataset déséquilibré

Variable duration disponible uniquement après contact

Risque de surapprentissage

Pistes d’amélioration

Application de SMOTE

Modèle temps réel sans duration

Tester le deep learning

Ajouter plus de variables comportementales

